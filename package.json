{
  "name": "trello-csv",
  "version": "2.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "cd dist && node index.js",
    "build": "rimraf dist && tsc && cd dist && mkdir input-folder && mkdir output-folder",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/exceljs": "^0.5.3",
    "exceljs": "^2.0.1",
    "rimraf": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^12.7.5",
    "typescript": "^3.6.3"
  }
}
